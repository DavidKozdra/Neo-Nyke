{
	"name": "neonyke-server",
	"version": "1.0.0",
	"private": true,
	"scripts": {
		"dev": "wrangler dev",
		"deploy": "wrangler deploy",
		"kv:create": "wrangler kv namespace create STORE",
		"kv:create-preview": "wrangler kv namespace create STORE --preview",
		"test": "node ../scripts/run-tests.js",
		"log": "node ./scripts/log-data.js"
	},
	"devDependencies": {
		"wrangler": "^4.92.0"
	}
}
