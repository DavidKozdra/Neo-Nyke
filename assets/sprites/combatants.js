window.NeoNykeSpriteDefs = {
  princess: {
    palette: { a: '#241026', b: '#d85fa8', c: '#8b4a2c', d: '#f0c830' },
    pixels: [
      '.ddddddd..',
      'ddcbbbcdd.',
      'dcbcccbcd.',
      '.dcccccd..',
      '.bcccccb..',
      '.bccbccb..',
      '.bcbcbcb..',
      '..b..bb...',
      '.bb..bb...',
      '.c....c...',
    ],
  },
  thorn_knight: {
    palette: { a: '#171d24', b: '#495765', c: '#dfe7ef', d: '#8b3247' },
    pixels: [
      '...cc.....',
      '..cbbc....',
      '..cbbc....',
      '..bbbbd...',
      '.bbccbdd..',
      '.bbbbbbd..',
      '.bbddbb...',
      '..b..bb...',
      '.bb..bb...',
      '.c....c...',
    ],
  },
  metao: {
    palette: { a: '#23192b', b: '#55406a', c: '#e4ddf2', d: '#35a99b' },
    pixels: [
      '....d.....',
      '...bbb....',
      '..bbbbb...',
      '..bcacb...',
      '..bcccbd..',
      '.bbbbbdd..',
      '.bdbbbb...',
      '..b..bb...',
      '.bb..bb...',
      '.c....c...',
    ],
  },
  granialla: {
    palette: { a: '#1e1018', b: '#6b3a22', c: '#c8824a', d: '#f5d84a' },
    pixels: [
      '.ddddddd..',
      'ddcbbbcdd.',
      'dcbcccbcd.',
      '.dcccccbd.',
      '.baaaaab..',
      '.baabbab..',
      '.bacbcab..',
      '..b..bb...',
      '.bb..bb...',
      '.c....c...',
    ],
  },
  mooggy: {
    palette: { a: '#101214', b: '#f4f4ef', c: '#050607', d: '#d31f35' },
    pixels: [
      '..c..c....',
      '.cbccbc...',
      '.baccab...',
      '.bbccbb...',
      '..bddb....',
      '.bddddb...',
      '.bdbbdb...',
      '..b..b....',
      '.bb..bb...',
      '.d....d...',
    ],
  },
  hunter: {
    palette: { a: '#1a242b', b: '#3f5a67', c: '#d6dcda', d: '#7a1f24' },
    pixels: [
      '...cc.....',
      '..cbbc....',
      '..bbbb....',
      '.bbddbb...',
      '.bbbbbb...',
      '..bbbbd...',
      '.bb..bdd..',
      '..b..bb...',
      '.bb..bb...',
      '..........',
    ],
  },
  cult_follower: {
    palette: { a: '#1f2129', b: '#6d576d', c: '#ece3ea', d: '#8a3148' },
    pixels: [
      '...cc.....',
      '..cbbc....',
      '..bbbb....',
      '.bbddbb...',
      '.bbbbbb...',
      '..bbbb....',
      '..bddb....',
      '..b..b....',
      '.bb..bb...',
      '.d....d...',
    ],
  },
  cult_mage: {
    palette: { a: '#20182a', b: '#5d4472', c: '#e1d8ef', d: '#34264f' },
    pixels: [
      '...dd.....',
      '..dbbd....',
      '..bbbb....',
      '..bcacbd..',
      '.bbcccbdd.',
      '.bbbbbbd..',
      '..bddb....',
      '..b..b....',
      '.bb..bb...',
      'c......c..',
    ],
  },
  sniper: {
    palette: { a: '#162126', b: '#4b6b77', c: '#e1ecee', d: '#775e3a' },
    pixels: [
      '...cc.....',
      '..cbbcdd..',
      '..bbbbbd..',
      '.bbddbbd..',
      '.bbbbbbd..',
      '..bbbbd...',
      '.bb..bdd..',
      '..b..bb...',
      '.db..bd...',
      '..........',
    ],
  },
  knave: {
    palette: { a: '#26181d', b: '#7b3648', c: '#f1d7dc', d: '#4a4d59' },
    pixels: [
      '....c.....',
      '...cbb....',
      '..cbbbb...',
      '..bbbbd...',
      '.bbddbdd..',
      '.bbbbbbd..',
      '..bbbb....',
      '..b..bb...',
      '.bb..bb...',
      '.d....d...',
    ],
  },
  charger: {
    palette: { a: '#2d1e13', b: '#9b5c2f', c: '#f2d7b7', d: '#57402a' },
    pixels: [
      '..d..d....',
      '..bbbb....',
      '.bcaccb...',
      '.bbbbbb...',
      'bbddddbb..',
      'bbbbbbbb..',
      '.bdbbbb...',
      '..b..bb...',
      '.db..bd...',
      '..........',
    ],
  },
  laser: {
    palette: { a: '#181626', b: '#5e5a8b', c: '#e4dcff', d: '#2d9d8f' },
    pixels: [
      '...dddd...',
      '..dbbbd...',
      '.dbcccbd..',
      '.bbcccbb..',
      '.bdddddb..',
      '.bbcccbb..',
      '.dbcccbd..',
      '..b...b...',
      '.cc...cc..',
      '..........',
    ],
  },
  golem: {
    palette: { a: '#34261b', b: '#8c6542', c: '#d2b68f', d: '#6d4f34' },
    pixels: [
      '..bbbb....',
      '.bcddcb...',
      '.bccccb...',
      'bbddddbb..',
      'bbbbbbbb..',
      '.bbccbb...',
      '.bb..bb...',
      '.bd..db...',
      '.d....d...',
      '..........',
    ],
  },
  bulk_golem: {
    palette: { a: '#3b291a', b: '#9b6b3e', c: '#e0bc84', d: '#735333' },
    pixels: [
      '.bbbbbb...',
      'bbccccbb..',
      'bbddddbb..',
      'bbbbbbbb..',
      'bbddddbb..',
      'bbbbbbbb..',
      '.bbccbb...',
      '.bb..bb...',
      '.dd..dd...',
      '..........',
    ],
  },
  queen_cult: {
    palette: { a: '#2a1b2b', b: '#a06a8d', c: '#f5dfef', d: '#6d274b' },
    pixels: [
      '.c.c.c....',
      '..bbbb....',
      '.bcaccb...',
      '.bccccb...',
      '.bbddbb...',
      'bbbbbbbb..',
      '.bbccbb...',
      '.db..bd...',
      'c.c..c.c..',
      '..........',
    ],
  },
  artificer_knave: {
    palette: { a: '#2f2118', b: '#b9854a', c: '#f4dfb9', d: '#7d5030' },
    pixels: [
      '..bbbb....',
      '.bcaccbd..',
      '.bccccbdd.',
      '.bbddbbd..',
      'bbbbbbbd..',
      '.bbddbb...',
      '.bd..db...',
      '..b..bb...',
      '.c....c...',
      '..........',
    ],
  },
  antony_blemmye: {
    palette: { a: '#221713', b: '#8b4f32', c: '#f0c69b', d: '#5e3628' },
    pixels: [
      '.dddddd...',
      'dbbccbbd..',
      'dbccccbd..',
      'dbbccbbd..',
      'ddbbbbdd..',
      'bbbbbbbb..',
      'bbddddbb..',
      '.bb..bb...',
      '.dd..dd...',
      '...cc.....',
    ],
  },
  handsome_devil: {
    palette: { a: '#1b1118', b: '#7a1f2d', c: '#ffd6c6', d: '#ff3348' },
    pixels: [
      '..d..d....',
      '.dcbbcdd..',
      '.bccccb...',
      '..bccb....',
      '.bbbbbb...',
      '.bbddbb...',
      '..bbbb....',
      '..b..b....',
      '.bb..bb...',
      '.d....d...',
    ],
  },
  god: {
    palette: { a: '#2c2e38', b: '#d8dbe4', c: '#ffffff', d: '#9ea6b6' },
    pixels: [
      '.c.c.c....',
      '..bbbb....',
      '.bcaccb...',
      '.bccccb...',
      '.bbddbb...',
      'bbbcbbbb..',
      '.bbccbb...',
      '.db..bd...',
      'c.c..c.c..',
      '....d.....',
    ],
  },
};

(() => {
  const defs = window.NeoNykeSpriteDefs || {};
  const WIDTH = 10;
  const HEIGHT = 10;

  function toGrid(pixels) {
    return pixels.map(row => row.padEnd(WIDTH, '.').slice(0, WIDTH).split(''));
  }

  function toPixels(grid) {
    return grid.map(row => row.join(''));
  }

  function countColors(pixels, yStart = 0, yEnd = HEIGHT) {
    const counts = {};
    for (let y = yStart; y < Math.min(yEnd, pixels.length); y += 1) {
      for (const pixel of pixels[y]) {
        if (pixel === '.') continue;
        counts[pixel] = (counts[pixel] || 0) + 1;
      }
    }
    return counts;
  }

  function commonColor(pixels, yStart, yEnd, fallback = 'b') {
    const counts = countColors(pixels, yStart, yEnd);
    return Object.keys(counts).sort((a, b) => counts[b] - counts[a])[0] || fallback;
  }

  function put(grid, x, y, color) {
    if (x < 0 || y < 0 || x >= WIDTH || y >= HEIGHT) return;
    grid[y][x] = color;
  }

  function clearLowerPose(grid) {
    for (let y = 7; y <= 9; y += 1) {
      for (let x = 1; x <= 8; x += 1) {
        if (grid[y][x] !== '.') grid[y][x] = '.';
      }
    }
  }

  function drawLegs(grid, body, accent, pose) {
    clearLowerPose(grid);
    if (pose === 'strideA') {
      [[3, 7], [6, 7], [2, 8], [3, 8], [6, 8], [7, 8], [2, 9], [4, 9], [7, 9]].forEach(([x, y]) => put(grid, x, y, body));
      put(grid, 2, 9, accent);
      put(grid, 7, 9, accent);
      return;
    }
    if (pose === 'strideB') {
      [[3, 7], [6, 7], [3, 8], [4, 8], [5, 8], [6, 8], [2, 9], [5, 9], [7, 9]].forEach(([x, y]) => put(grid, x, y, body));
      put(grid, 2, 9, accent);
      put(grid, 7, 9, accent);
      return;
    }
    [[3, 7], [6, 7], [2, 8], [3, 8], [6, 8], [7, 8], [2, 9], [7, 9]].forEach(([x, y]) => put(grid, x, y, body));
    put(grid, 2, 9, accent);
    put(grid, 7, 9, accent);
  }

  function drawArmPose(grid, body, accent, pose) {
    const lead = accent || body;
    if (pose === 'strideA') {
      [[1, 4, body], [1, 5, body], [8, 5, lead], [8, 6, lead]].forEach(([x, y, color]) => put(grid, x, y, color));
      return;
    }
    if (pose === 'strideB') {
      [[1, 6, lead], [1, 5, lead], [8, 4, body], [8, 5, body]].forEach(([x, y, color]) => put(grid, x, y, color));
      return;
    }
    if (pose === 'ready') {
      [[1, 5, body], [2, 6, body], [7, 5, lead], [8, 6, lead]].forEach(([x, y, color]) => put(grid, x, y, color));
      return;
    }
    [[1, 5, body], [8, 5, body]].forEach(([x, y, color]) => put(grid, x, y, color));
  }

  function shiftRows(grid, rows, dx) {
    rows.forEach(y => {
      const next = Array(WIDTH).fill('.');
      for (let x = 0; x < WIDTH; x += 1) {
        const pixel = grid[y][x];
        if (pixel === '.') continue;
        const nx = Math.max(0, Math.min(WIDTH - 1, x + dx));
        if (next[nx] === '.') next[nx] = pixel;
      }
      grid[y] = next;
    });
  }

  function blinkFace(grid, pixels, face, lid) {
    const candidates = [];
    for (let y = 1; y <= 3; y += 1) {
      for (let x = 2; x <= 7; x += 1) {
        if (pixels[y]?.[x] === face) candidates.push({ x, y });
      }
    }
    if (!candidates.length) return;
    const rowCounts = candidates.reduce((counts, point) => {
      counts[point.y] = (counts[point.y] || 0) + 1;
      return counts;
    }, {});
    const blinkY = Number(Object.keys(rowCounts).sort((a, b) => rowCounts[b] - rowCounts[a])[0]);
    candidates.filter(point => point.y === blinkY).forEach(point => {
      put(grid, point.x, point.y, lid);
    });
  }

  function makeIdleFrame(pixels, pose) {
    const grid = toGrid(pixels);
    const body = commonColor(pixels, 4, 8, 'b');
    const accent = commonColor(pixels, 8, 10, body);
    const face = commonColor(pixels, 1, 4, 'c');
    const lid = commonColor(pixels, 4, 7, body);
    if (pose === 'breathe') {
      drawArmPose(grid, body, accent, 'center');
      drawLegs(grid, body, accent, 'center');
      put(grid, 2, 6, body);
      put(grid, 7, 6, body);
      return toPixels(grid);
    }
    if (pose === 'ready') {
      drawArmPose(grid, body, accent, 'ready');
      drawLegs(grid, body, accent, 'center');
      return toPixels(grid);
    }
    if (pose === 'blink') {
      drawArmPose(grid, body, accent, 'center');
      drawLegs(grid, body, accent, 'center');
      blinkFace(grid, pixels, face, lid);
      return toPixels(grid);
    }
    return toPixels(grid);
  }

  function makeWalkFrame(pixels, pose) {
    const grid = toGrid(pixels);
    const body = commonColor(pixels, 4, 8, 'b');
    const accent = commonColor(pixels, 8, 10, body);
    drawArmPose(grid, body, accent, pose);
    drawLegs(grid, body, accent, pose);
    return toPixels(grid);
  }

  function makeAttackFrame(pixels, pose) {
    const grid = toGrid(pixels);
    const body = commonColor(pixels, 4, 8, 'b');
    const bright = commonColor(pixels, 0, 4, 'c');
    const accent = commonColor(pixels, 4, 10, 'd');
    if (pose === 'windup') {
      put(grid, 0, 6, body);
      put(grid, 1, 5, body);
      put(grid, 2, 4, body);
      put(grid, 7, 4, accent);
      put(grid, 8, 3, bright);
      put(grid, 9, 2, bright);
      return toPixels(grid);
    }
    if (pose === 'strike') {
      put(grid, 5, 3, body);
      put(grid, 6, 4, body);
      put(grid, 7, 4, body);
      put(grid, 8, 4, accent);
      put(grid, 9, 4, bright);
      put(grid, 7, 5, accent);
      put(grid, 8, 3, bright);
      put(grid, 9, 2, bright);
      put(grid, 9, 6, bright);
      return toPixels(grid);
    }
    put(grid, 1, 5, body);
    put(grid, 2, 6, body);
    put(grid, 5, 5, body);
    put(grid, 6, 6, body);
    put(grid, 7, 6, accent);
    put(grid, 8, 7, bright);
    put(grid, 9, 8, bright);
    return toPixels(grid);
  }

  Object.keys(defs).forEach(key => {
    const def = defs[key];
    if (!def?.pixels || def.frames) return;
    def.frames = {
      idle0: makeIdleFrame(def.pixels, 'breathe'),
      idle1: makeIdleFrame(def.pixels, 'ready'),
      idleBlink: makeIdleFrame(def.pixels, 'blink'),
      walk0: makeWalkFrame(def.pixels, 'strideA'),
      walk1: makeWalkFrame(def.pixels, 'center'),
      walk2: makeWalkFrame(def.pixels, 'strideB'),
      attack0: makeAttackFrame(def.pixels, 'windup'),
      attack1: makeAttackFrame(def.pixels, 'strike'),
      attack2: makeAttackFrame(def.pixels, 'recover'),
    };
    def.animations = {
      idle: ['idle0', 'idle1', 'idle0', 'idle'],
      blink: ['idleBlink'],
      walk: ['walk0', 'walk1', 'walk2', 'walk1'],
      attack: ['attack0', 'attack1', 'attack2'],
    };
  });
})();
